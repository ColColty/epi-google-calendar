version: "2"

services:
  web:
    build: .
    depends_on:
    - mongo
    environment:
      - AUTO_LOGIN=
      - CALENDAR_ID=
      - TIME_ZONE=
      - IMPORTANT_EVENT_COLOR_ID=
    restart: always

  mongo:
    image: mongo
    ports:
      - "27017:27017"
    restart: always